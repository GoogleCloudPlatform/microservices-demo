apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

components:
  # Production: remove load generator (no synthetic traffic in prod)
  - ../../components/without-loadgenerator
  # Production: enable network policies for isolation
  - ../../components/network-policies
