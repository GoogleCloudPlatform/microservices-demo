apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base

# Staging: includes load generator to simulate traffic
# No additional components needed for basic staging environment
