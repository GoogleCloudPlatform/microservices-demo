apiVersion: v1
kind: Service
metadata:
  name: cartservice
  namespace: staging
  labels:
    app: cartservice
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: cartservice
    environment: staging
  ports:
    - name: grpc
      protocol: TCP
      port: 8080
      targetPort: 8080