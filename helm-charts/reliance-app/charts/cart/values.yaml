replicaCount: 1
environment: staging

image:
  repository: uadeel421/reliance-cartservice
  pullPolicy: Always
  tag: "latest"

service:
  port: 8080

nodeSelector:
  pool: stgpool

# This secret will be created from the values below
secrets:
  CartStore: "AzureMySQL"
  AZURE_MYSQL_CONNECTION_STRING: "Server=rwdbserver.mysql.database.azure.com;Port=3306;Database=hipstershop;Uid=kpadmin;Pwd=p1Kistan0978^;SslMode=Required;"

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "256Mi"
